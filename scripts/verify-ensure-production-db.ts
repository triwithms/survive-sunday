/**
 * Moved. Destructive against whatever DATABASE_URL is set.
 *
 *   tsx scripts/_dangerous/verify-ensure-production-db.ts
 */
console.error(
  "Moved to scripts/_dangerous/verify-ensure-production-db.ts — not a casual script."
);
process.exit(1);
