/**
 * Moved. Not a Vercel build helper — do not wire this to `npm run build`.
 *
 * Manual/emergency:
 *   tsx scripts/_dangerous/ensure-production-db.ts
 * Production Neon requires ALLOW_PROD_DB_MUTATION=1.
 */
console.error(
  "Moved to scripts/_dangerous/ensure-production-db.ts — not a Vercel build helper."
);
process.exit(1);
