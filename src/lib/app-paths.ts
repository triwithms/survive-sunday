/** Signed-in landing and kept deep-link URLs (labels live in nav). */

export const DEFAULT_SIGNED_IN_PATH = "/pick";
export const SELECTIONS_PATH = "/pool";
export const LEADERBOARD_PATH = "/standings";
export const NFL_STANDINGS_PATH = "/nfl";
